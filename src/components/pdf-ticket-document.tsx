// This file is no longer used for PDF generation and is kept to avoid breaking imports.
// The ticket download functionality has been moved to client-side HTML generation
// in `digital-ticket.tsx`.

const PdfTicketDocument = () => {
  return null;
};

export default PdfTicketDocument;
